---
"@stoneforge/smithy": minor
---

Add agent tool permission model with auto-allowed tools, configurable bash command allowlist, approval request service with SQLite storage, API endpoints for managing approval requests, and session-level permission monitoring.
