---
"@stoneforge/quarry": minor
---

Add workflow preset selection to `sf init` with three presets (Auto, Review, Approve) that configure merge, workflow, and agent permission settings. Supports interactive prompt and `--preset` CLI flag.
