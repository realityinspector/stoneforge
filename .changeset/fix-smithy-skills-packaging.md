---
"@stoneforge/smithy": patch
---

Add skills directory to build output (`cp -r src/skills dist/skills`) and add `./skills` entry to package exports map so skills are discoverable in published packages.
